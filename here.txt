NSBEdit handover notes
Written: 2026-06-17

Current status
- The installer pause at the end was removed, so the package should no longer sit there waiting for Enter after the Ollama model pulls finish.
- The AI shell still does not render Ollama replies faithfully yet. Ollama answers, but the NSBEdit pane is still losing code colours, links, and copy-code affordances that the Ollama app shows live while the reply arrives.
- The current AI work is focused on making the pane display Ollama output as-is, not flattening it to plain text or dropping the visual cues that make code usable.
- The AI module still needs a single consistent language sweep, but the immediate blocker is the rendering fidelity of the reply pane.

What to do next
- Keep the reply renderer focused on faithfully showing what Ollama sends, including code colouring, links, and copy affordances.
- If the AI pane still stalls, revisit the send/completion path after the versioned docs are updated and rebuilt.
- Update the changelog, README, and API docs under the new version tag before committing.

Notes
- Ollama itself is answering; the open problem is our answer pane fidelity.
- The Ollama app is still the reference for the desired behaviour: live rendering, colours, links, and code affordances as the reply arrives.
- Keep local AI and cloud AI flows separate.
- Keep the model picker realtime, not restart-based.