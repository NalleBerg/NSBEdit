NSBEdit handover notes
Written: 2026-06-26

Current status
- The AI answer now renders as a single RichEdit page and the final output is query-only for now.
- The query text is visible in the rendered page, and the next step is to add the answer content one part at a time without losing the first block again.
- The renderer process is intentionally being built in small ordered pieces: make one part perfect, then add the next part, then repeat until the page stays stable.
- The visible Copy code affordance still exists for the later code step, but the current focus is getting the page order right before bringing back the next block.
- Rendering correctness is the priority for Suggest mode; more advanced mixed pages only come after the basic sequential page render is solid.

What was fixed in this session
- The build still succeeds with the current changes.
- The query-only final render is working and the first visible block is now the query.
- The answer host scroll behavior and the RichEdit render path remain intact.

What needs fixing next
- Add the next answer block after the query while keeping the first block visible.
- Keep the Copy code header and later Scintilla syntax coloring working when code blocks are reintroduced.
- Prevent the viewport from jumping away from the first block when more content is appended.
- Remove any extra scrollbar when the content fits inside the pane.

Suggested continuance
- Resume in ollama_ai.cpp from Ai_RenderMarkdownReply and the answer append helpers.
- Build the page incrementally: first block perfect, then next block, then code, then more advanced mixed pages.
- After that, refresh the release docs, API notes, and README together before the next commit.

Release notes
- MyStyle remains the main documentation tree for API_INTERNALS/API work.