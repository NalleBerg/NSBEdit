NSBEdit handover notes
Written: 2026-06-22

Current status
- The AI reply path is still not producing visible output in the app, even though the spinner now stops when the model starts writing the answer.
- The renderer/build work is not finished yet, so the current problem remains unresolved.
- cmark-gfm is now linked into the build, and the next pass should focus on why the reply text is not appearing correctly in the answer pane.
- The close-window focus handoff work is still relevant and should stay in the release notes once the AI display path is stable again.

What needs fixing next
- Make the AI answer actually show in the app again, then verify whether the live prose and code-block split renderer is painting with the right colors.
- Confirm the copy-code and Scintilla code-block path still behaves once text is visible.
- Keep the main window focus restore behavior in place when the AI window closes.

Suggested continuance
- Resume from the answer-pane rendering path in ollama_ai.cpp and check where the visible text is being lost.
- Once that is stable, refresh the release docs, API notes, and README together before the next commit.

Release notes
- MyStyle remains the main documentation tree for API_INTERNALS/API work.